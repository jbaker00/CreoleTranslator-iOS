# Project

This project is configured to help you avoid committing private information (like API keys) to Git.

## Environment variables

Store secrets in a local `.env` file that is ignored by Git. Do not commit it.

Create your own `.env` by copying the example and filling in values:

